{
  "name": "gigfinder",
  "version": "1.0.0",
  "description": "GigFinder - Connect musicians and venues",
  "scripts": {
    "front": "cd front && npm run dev",
    "back": "cd back && deno run --allow-all src/index.ts",
    "dev": "npm-run-all --parallel front back"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
